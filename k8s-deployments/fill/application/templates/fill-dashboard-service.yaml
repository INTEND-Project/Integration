apiVersion: v1
kind: Service
metadata:
  name: fill-dashboard
spec:
  selector:
    app: fill-dashboard
  ports:
    - port: {{ .Values.fillDashboard.port }}
      targetPort: {{ .Values.fillDashboard.port }}