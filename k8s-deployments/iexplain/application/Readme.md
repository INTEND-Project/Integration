# Install iExplain

## Create secret for OPENAI_API_KEY

```bash
kubectl create secret generic openai-api-key --from-literal=OPENAI_API_KEY=<KEY>
```

## Install ArgoCD application from web interface
