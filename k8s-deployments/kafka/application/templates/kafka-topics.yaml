{{- range $name, $config := .Values.topics }}
apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: {{ $name }}  
  labels:
    strimzi.io/cluster: {{ $.Values.kafka.name }}
spec:
  partitions: {{ $config.partitions }}
  replicas: {{ $config.replicas }}
---
{{- end }}