apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-intent-translator

spec:
  type: ClusterIP

  selector:
    app: {{ .Release.Name }}-intent-translator

  ports:
    - name: http
      port: 8080
      targetPort: 8080