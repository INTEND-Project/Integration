apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-operation-manager

spec:
  type: ClusterIP

  selector:
    app: {{ .Release.Name }}-operation-manager

  ports:
    - name: http
      port: 8080
      targetPort: 8080