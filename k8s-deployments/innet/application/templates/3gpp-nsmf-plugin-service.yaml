apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-3gpp-nsmf-plugin

spec:
  type: ClusterIP

  selector:
    app: {{ .Release.Name }}-3gpp-nsmf-plugin

  ports:
    - name: http
      port: 8084
      targetPort: 8080