apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-intent-lcm-backend

spec:
  type: ClusterIP

  selector:
    app: {{ .Release.Name }}-intent-lcm-backend

  ports:
    - name: http
      port: 8080
      targetPort: 8080